language: android
android:
  components:
    - android-27
    - build-tools-27.0.3


script:
 - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then cd $TRAVIS_BUILD_DIR ; fi
  - which infer
  - infer --version
  - ./gradlew build --stacktrace
